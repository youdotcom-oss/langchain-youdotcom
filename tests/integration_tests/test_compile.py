"""Placeholder integration test — verifies the package compiles."""

import pytest


@pytest.mark.compile
def test_placeholder() -> None:
    """Placeholder so the integration test suite is not empty."""
